sections
  =Meta/Head/Cut

headers
# Header1  <-- Title if at start
## Header2
## Header with id [id]
## Header with optional ##

reference here:
  [#id]

inline code:
  `code`
  all special chars inside code are replaced inside `...`.
  `<i>code</i>` -> `&lt;i&gt;code&lt;/i&gt;`
  to use tags inside code, use simple tags: <code><i>code</i></code>

internal links:
  [title](url)
  [title](#id)
  ["title with [ ] and \" "](...)
  all internal links - with [#id], important for reorganization. If you want to reference - set id

external links
  [http://...](text?)

no format inside:
  [noformat]...[/noformat]



Navigation forms navigation from
01-* files, where 01 is any number
file must not end with .bak

Java Applet writes to .applet.json.
Only can read directory structure.